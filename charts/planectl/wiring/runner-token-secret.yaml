# Gitea Actions runner registration token.
# Obtained from the Gitea API by the init Job; applied here by the wiring Job.
apiVersion: v1
kind: Secret
metadata:
  name: gitea-runner-token
  namespace: $GITEA_NS
type: Opaque
stringData:
  token: $RUNNER_TOKEN
