apiVersion: v1
kind: Service
metadata:
  name: {{ include "planectl.fullname" . }}
  labels:
    {{- include "planectl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "planectl.selectorLabels" . | nindent 4 }}
